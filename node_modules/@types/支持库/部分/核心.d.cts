/*! *****************************************************************************
Copyright (c) Microsoft Corporation. All rights reserved.
Licensed under the Apache License, Version 2.0 (the "License"); you may not use
this file except in compliance with the License. You may obtain a copy of the
License at http://www.apache.org/licenses/LICENSE-2.0
 
THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
MERCHANTABLITY OR NON-INFRINGEMENT.
 
See the Apache Version 2.0 License for specific language governing permissions
and limitations under the License.
***************************************************************************** */
/// <引用 不使用默认支持库= "真"/>
//@@{ 属性键类:PropertyKey }
声明 类型 属性键类 = 文字 | 数字 | 符号;
//@@{ 数组类:Array }
接口 数组类<T> {
    /**
     * Returns the value of the first element in the array where predicate is true, and undefined
     * otherwise.
     * @param 条件 find calls predicate once for each element of the array, in ascending
     * order, until it finds one where predicate returns true. If such an element is found, find
     * immediately returns that element value. Otherwise, find returns undefined.
     * @param 本体参数 If provided, it will be used as the this value for each invocation of
     * predicate. If it is not provided, undefined is used instead.
     */
    //@@{ 查找:find, 条件:predicate } 
    //@@{ 值:value, 索引:index, 对象:obj, 本体参数:thisArg }
    查找<S 扩展 T>(条件: (本体: 无值, 值: T, 索引: 数字, 对象: T[]) => 值 作为 S, 本体参数?: 任意): S | 未定;
    //@@{ 查找:find, 条件:predicate } 
    //@@{ 值:value, 索引:index, 对象:obj, 本体参数:thisArg }
    查找(条件: (值: T, 索引: 数字, 对象: T[]) => 真假, 本体参数?: 任意): T | 未定;
    /**
     * Returns the index of the first element in the array where predicate is true, and -1
     * otherwise.
     * @param 条件 find calls predicate once for each element of the array, in ascending
     * order, until it finds one where predicate returns true. If such an element is found,
     * findIndex immediately returns that element index. Otherwise, findIndex returns -1.
     * @param 本体参数 If provided, it will be used as the this value for each invocation of
     * predicate. If it is not provided, undefined is used instead.
     */
    //@@{ 查找索引:findIndex, 条件:predicate, 值:value, 索引:index, 对象:obj, 本体参数:thisArg }
    查找索引(条件: (值: T, 索引: 数字, 对象: T[]) => 真假, 本体参数?: 任意): 数字;
    /**
     * Returns the this object after filling the section identified by start and end with value
     * @param 值 value to fill array section with
     * @param 开始 index to start filling the array at. If start is negative, it is treated as
     * length+start where length is the length of the array.
     * @param 结束 index to stop filling the array at. If end is negative, it is treated as
     * length+end.
     */
    //@@{ 填充:fill, 值:value, 开始:start, 结束:end,  }
    填充(值: T, 开始?: 数字, 结束?: 数字): 本体;
    /**
     * Returns the this object after copying a section of the array identified by start and end
     * to the same array starting at position target
     * @param 目标 If target is negative, it is treated as length+target where length is the
     * length of the array.
     * @param 开始 If start is negative, it is treated as length+start. If end is negative, it
     * is treated as length+end.
     * @param 结束 If not specified, length of the this object is used as its default value.
     */
    //@@{ 复制宽度:copyWithin, 目标:target, 开始:start, 结束:end }
    复制宽度(目标: 数字, 开始: 数字, 结束?: 数字): 本体;
}
//@@{ 数组构造函数类:ArrayConstructor, 从连接:from, 数组连接:arrayLike, 映射函数:mapfn, 值:v, 键:k, 本体参数:thisArg, 以元素:of, 项目:items }
接口 数组构造函数类 {
    /**
     * Creates an array from an array-like object.
     * @param 数组连接 An array-like object to convert to an array.
     * @param 映射函数 A mapping function to call on every element of the array.
     * @param 本体参数 Value of 'this' used to invoke the mapfn.
     */
    //@@{ 数组连接类:ArrayLike }
    从连接<T, U = T>(数组连接: 数组连接类<T>, 映射函数?: (值: T, 键: 数字) => U, 本体参数?: 任意): U[];
    /**
     * Returns a new array from a set of elements.
     * @param 项目 A set of elements to include in the new array object.
     */
    以元素<T>(...项目: T[]): T[];
}
//@@{ 时间日期类:Date }
//@@{ 时间日期构造函数类:DateConstructor }
//@@{ 值:value }
接口 时间日期构造函数类 {
    新建 (值: 时间日期类): 时间日期类;
}
//@@{ 函数类:Function }
接口 函数类 {
    /**
     * Returns the name of the function. Function names are read-only and can not be changed.
     */
    //@@{ 名字:name }
    只读 名字: 文字;
}
//@@{ 数学类:Math }
接口 数学类 {
    /**
     * Returns the number of leading zero bits in the 32-bit binary representation of a number.
     * @param x A numeric expression.
     */
    clz32(x: 数字): 数字;
    /**
     * Returns the result of 32-bit multiplication of two numbers.
     * @param x First number
     * @param y Second number
     */
    imul(x: 数字, y: 数字): 数字;
    /**
     * Returns the sign of the x, indicating whether x is positive, negative or zero.
     * @param x The numeric expression to test
     */
    sign(x: 数字): 数字;
    /**
     * Returns the base 10 logarithm of a number.
     * @param x A numeric expression.
     */
    log10(x: 数字): 数字;
    /**
     * Returns the base 2 logarithm of a number.
     * @param x A numeric expression.
     */
    log2(x: 数字): 数字;
    /**
     * Returns the natural logarithm of 1 + x.
     * @param x A numeric expression.
     */
    log1p(x: 数字): 数字;
    /**
     * Returns the result of (e^x - 1) of x (e raised to the power of x, where e is the base of
     * the natural logarithms).
     * @param x A numeric expression.
     */
    expm1(x: 数字): 数字;
    /**
     * Returns the hyperbolic cosine of a number.
     * @param x A numeric expression that contains an angle measured in radians.
     */
    cosh(x: 数字): 数字;
    /**
     * Returns the hyperbolic sine of a number.
     * @param x A numeric expression that contains an angle measured in radians.
     */
    sinh(x: 数字): 数字;
    /**
     * Returns the hyperbolic tangent of a number.
     * @param x A numeric expression that contains an angle measured in radians.
     */
    tanh(x: 数字): 数字;
    /**
     * Returns the inverse hyperbolic cosine of a number.
     * @param x A numeric expression that contains an angle measured in radians.
     */
    acosh(x: 数字): 数字;
    /**
     * Returns the inverse hyperbolic sine of a number.
     * @param x A numeric expression that contains an angle measured in radians.
     */
    asinh(x: 数字): 数字;
    /**
     * Returns the inverse hyperbolic tangent of a number.
     * @param x A numeric expression that contains an angle measured in radians.
     */
    atanh(x: 数字): 数字;
    /**
     * Returns the square root of the sum of squares of its arguments.
     * @param values Values to compute the square root for.
     *     If no arguments are passed, the result is +0.
     *     If there is only one argument, the result is the absolute value.
     *     If any argument is +Infinity or -Infinity, the result is +Infinity.
     *     If any argument is NaN, the result is NaN.
     *     If all arguments are either +0 or −0, the result is +0.
     */
    hypot(...values: 数字[]): 数字;
    /**
     * Returns the integral part of the a numeric expression, x, removing any fractional digits.
     * If x is already an integer, the result is x.
     * @param x A numeric expression.
     */
    trunc(x: 数字): 数字;
    /**
     * Returns the nearest single precision float representation of a number.
     * @param x A numeric expression.
     */
    fround(x: 数字): 数字;
    /**
     * Returns an implementation-dependent approximation to the cube root of number.
     * @param x A numeric expression.
     */
    cbrt(x: 数字): 数字;
}
//@@{ 数字构造函数类:NumberConstructor }
接口 数字构造函数类 {
    /**
     * The value of Number.EPSILON is the difference between 1 and the smallest value greater than 1
     * that is representable as a Number value, which is approximately:
     * 2.2204460492503130808472633361816 x 10‍−‍16.
     */
    //@@{ _极小值_:EPSILON }
    只读 _极小值_: 数字;
    /**
     * Returns true if passed value is finite.
     * Unlike the global isFinite, Number.isFinite doesn't forcibly convert the parameter to a
     * number. Only finite values of the type number, result in true.
     * @param 数字 A numeric value.
     */
    //@@{ 是有限的:isFinite, 数字:number }
    是有限的(数字: 数字): 真假;
    /**
     * Returns true if the value passed is an integer, false otherwise.
     * @param 数字 A numeric value.
     */
    //@@{ 是整数:isInteger, 数字:number }
    是整数(数字: 数字): 真假;
    /**
     * Returns a Boolean value that indicates whether a value is the reserved value NaN (not a
     * number). Unlike the global isNaN(), Number.isNaN() doesn't forcefully convert the parameter
     * to a number. Only values of the type number, that are also NaN, result in true.
     * @param 数字 A numeric value.
     */
    //@@{ 是非数字:isNaN, 数字:number }
    是非数字(数字: 数字): 真假;
    /**
     * Returns true if the value passed is a safe integer.
     * @param 数字 A numeric value.
     */
    //@@{ 是安全的整数:isSafeInteger, 数字:number }
    是安全的整数(数字: 数字): 真假;
    /**
     * The value of the largest integer n such that n and n + 1 are both exactly representable as
     * a Number value.
     * The value of Number.MAX_SAFE_INTEGER is 9007199254740991 2^53 − 1.
     */
    //@@{ _最大_安全数_:MAX_SAFE_INTEGER }
    只读 _最大_安全数_: 数字;
    /**
     * The value of the smallest integer n such that n and n − 1 are both exactly representable as
     * a Number value.
     * The value of Number.MIN_SAFE_INTEGER is −9007199254740991 (−(2^53 − 1)).
     */
    //@@{ _最小_安全数_:MIN_SAFE_INTEGER }
    只读 _最小_安全数_: 数字;
    /**
     * Converts a string to a floating-point number.
     * @param 文字 A string that contains a floating-point number.
     */
    //@@{ 编译浮点:parseFloat, 文字:string }
    编译浮点(文字: 文字): 数字;
    /**
     * Converts A string to an integer.
     * @param 文字 A string to convert into a number.
     * @param 基数 A value between 2 and 36 that specifies the base of the number in numString.
     * If this argument is not supplied, strings with a prefix of '0x' are considered hexadecimal.
     * All other strings are considered decimal.
     */
    //@@{ 编译整数:parseInt, 文字:string, 基数:radix }
    编译整数(文字: 文字, 基数?: 数字): 数字;
}
//@@{ 对象类:Object }
接口 对象类 {
    /**
     * Determines whether an object has a property with the specified name.
     * @param v A property name.
     */
    //@@{ 有自身属性:hasOwnProperty }
    有自身属性(v: 属性键类): 真假;
    /**
     * Determines whether a specified property is enumerable.
     * @param v A property name.
     */
    //@@{ 属性是可枚举:propertyIsEnumerable }
    属性是可枚举(v: 属性键类): 真假;
}
//@@{ 对象构造函数类:ObjectConstructor }
接口 对象构造函数类 {
    /**
     * Copy the values of all of the enumerable own properties from one or more source objects to a
     * target object. Returns the target object.
     * @param 目标 The target object to copy to.
     * @param 源 The source object from which to copy properties.
     */
    //@@{ 解构合并:assign, 目标:target, 源:source }
    解构合并<T, U>(目标: T, 源: U): T & U;
    /**
     * Copy the values of all of the enumerable own properties from one or more source objects to a
     * target object. Returns the target object.
     * @param 目标 The target object to copy to.
     * @param 源1 The first source object from which to copy properties.
     * @param 源2 The second source object from which to copy properties.
     */ 
    //@@{ 解构合并:assign, 目标:target, 源1:source1, 源2:source2 }
    解构合并<T, U, V>(目标: T, 源1: U, 源2: V): T & U & V;
    /**
     * Copy the values of all of the enumerable own properties from one or more source objects to a
     * target object. Returns the target object.
     * @param 目标 The target object to copy to.
     * @param 源1 The first source object from which to copy properties.
     * @param 源2 The second source object from which to copy properties.
     * @param 源3 The third source object from which to copy properties.
     */
    //@@{ 解构合并:assign, 目标:target, 源1:source1, 源2:source2, 源3:source3 }
    解构合并<T, U, V, W>(目标: T, 源1: U, 源2: V, 源3: W): T & U & V & W;
    /**
     * Copy the values of all of the enumerable own properties from one or more source objects to a
     * target object. Returns the target object.
     * @param 目标 The target object to copy to.
     * @param 源 One or more source objects from which to copy properties
     */
    //@@{ 解构合并:assign, 目标:target, 源:source }
    解构合并(目标: 实例, ...源: 任意[]): 任意;
    /**
     * Returns an array of all symbol properties found directly on object o.
     * @param 对象 Object to retrieve the symbols from.
     */
    //@@{ 取自身属性符号集:getOwnPropertySymbols, 对象:o }
    取自身属性符号集(对象: 任意): 符号[];
    /**
     * Returns true if the values are the same value, false otherwise.
     * @param 值1 The first value.
     * @param 值2 The second value.
     */
    //@@{ 作为:is, 值1:value1, 值2:value2 }
    作为(值1: 任意, 值2: 任意): 真假;
    /**
     * Sets the prototype of a specified object o to  object proto or null. Returns the object o.
     * @param 对象 The object to change its prototype.
     * @param 属性 The value of the new prototype or null.
     */
    //@@{ 置原型在:setPrototypeOf, 对象:o, 属性:proto }
    置原型在(对象: 任意, 属性: 实例 | 空值): 任意;
    /**
     * Gets the own property descriptor of the specified object.
     * An own property descriptor is one that is defined directly on the object and is not
     * inherited from the object's prototype.
     * @param 对象 Object that contains the property.
     * @param p Name of the property.
     */
    //@@{ 属性描述符类:PropertyDescriptor }
    //@@{ 得到自身属性描述符:getOwnPropertyDescriptor }
    //@@{ 对象:o, 属性键:propertyKey }
    得到自身属性描述符(对象: 任意, 属性键: 属性键类): 属性描述符类 | 未定;
    /**
     * Adds a property to an object, or modifies attributes of an existing property.
     * @param 对象 Object on which to add or modify the property. This can be a native JavaScript
     * object (that is, a user-defined object or a built in object) or a DOM object.
     * @param p The property name.
     * @param 特性集 Descriptor for the property. It can be for a data property or an accessor
     *  property.
     */
    //@@{ 属性描述符类:PropertyDescriptor }
    //@@{ 定义属性:defineProperty }
    //@@{ 对象:o, 属性键:propertyKey, 特性集:attributes }
    定义属性(对象: 任意, 属性键: 属性键类, 特性集: 属性描述符类): 任意;
}
//@@{ 只读数组类:ReadonlyArray }
接口 只读数组类<T> {
    /**
     * Returns the value of the first element in the array where predicate is true, and undefined
     * otherwise.
     * @param 条件 find calls predicate once for each element of the array, in ascending
     * order, until it finds one where predicate returns true. If such an element is found, find
     * immediately returns that element value. Otherwise, find returns undefined.
     * @param 本体参数 If provided, it will be used as the this value for each invocation of
     * predicate. If it is not provided, undefined is used instead.
     */
    //@@{ 只读数组类:ReadonlyArray }
    //@@{ 查找:find, 条件:predicate } 
    //@@{ 值:value, 索引:index,对象:obj, 本体参数:thisArg }
    查找<S 扩展 T>(条件: (本体: 无值, 值: T, 索引: 数字, 对象: 只读数组类<T>) => 值 作为 S, 本体参数?: 任意): S | 未定;
    //@@{ 只读数组类:ReadonlyArray }
    //@@{ 条件:predicate }
    //@@{ 值:value, 索引:index, 对象:obj,本体参数:thisArg }
    查找(条件: (值: T, 索引: 数字, 对象: 只读数组类<T>) => 真假, 本体参数?: 任意): T | 未定;
    /**
     * Returns the index of the first element in the array where predicate is true, and -1
     * otherwise.
     * @param 条件 find calls predicate once for each element of the array, in ascending
     * order, until it finds one where predicate returns true. If such an element is found,
     * findIndex immediately returns that element index. Otherwise, findIndex returns -1.
     * @param 本体参数 If provided, it will be used as the this value for each invocation of
     * predicate. If it is not provided, undefined is used instead.
     */
    //@@{ 只读数组类:ReadonlyArray }
    //@@{ 查找索引:findIndex, 条件:predicate, 值:value, 索引:index, 对象:obj, 本体参数:thisArg }
    查找索引(条件: (值: T, 索引: 数字, 对象: 只读数组类<T>) => 真假, 本体参数?: 任意): 数字;
}
//@@{ 正则表达式类:RegExp }
接口 正则表达式类 {
    /**
     * Returns a string indicating the flags of the regular expression in question. This field is read-only.
     * The characters in this string are sequenced and concatenated in the following order:
     *
     *    - "g" for global
     *    - "i" for ignoreCase
     *    - "m" for multiline
     *    - "u" for unicode
     *    - "y" for sticky
     *
     * If no flags are set, the value is the empty string.
     */
    //@@{ 旗帜:flags }
    只读 旗帜: 文字;
    /**
     * Returns a Boolean value indicating the state of the sticky flag (y) used with a regular
     * expression. Default is false. Read-only.
     */
    //@@{ 粘滞匹配:sticky }
    只读 粘滞匹配: 真假;
    /**
     * Returns a Boolean value indicating the state of the Unicode flag (u) used with a regular
     * expression. Default is false. Read-only.
     */
    //@@{ 统一码:unicode }
    只读 统一码: 真假;
}
//@@{ 正则表达式类:RegExp }
//@@{ 正则表达式构造函数类:RegExpConstructor }
//@@{ 模式:pattern, 旗帜:flags }
接口 正则表达式构造函数类 {
    新建 (模式: 正则表达式类, 旗帜?: 文字): 正则表达式类;
    (模式: 正则表达式类, 旗帜?: 文字): 正则表达式类;
}
//@@{ 文字类:String }
接口 文字类 {
    /**
     * Returns a nonnegative integer Number less than 1114112 (0x110000) that is the code point
     * value of the UTF-16 encoded code point starting at the string element at position pos in
     * the String resulting from converting this object to a String.
     * If there is no element at that position, the result is undefined.
     * If a valid UTF-16 surrogate pair does not begin at pos, the result is the code unit at pos.
     */
    //@@{ 代码点在:codePointAt, 前:pos }
    代码点在(前: 数字): 数字 | 未定;
    /**
     * Returns true if searchString appears as a substring of the result of converting this
     * object to a String, at one or more positions that are
     * greater than or equal to position; otherwise, returns false.
     * @param 搜索文字 search string
     * @param 位置 If position is undefined, 0 is assumed, so as to search all of the String.
     */
    //@@{ 包括:includes, 搜索文字:searchString, 位置:position }
    包括(搜索文字: 文字, 位置?: 数字): 真假;
    /**
     * Returns true if the sequence of elements of searchString converted to a String is the
     * same as the corresponding elements of this object (converted to a String) starting at
     * endPosition – length(this). Otherwise returns false.
     */
    //@@{ 结尾包含:endsWith, 搜索文字:searchString, 结尾位置:endPosition }
    结尾包含(搜索文字: 文字, 结尾位置?: 数字): 真假;
    /**
     * Returns the String value result of normalizing the string into the normalization form
     * named by form as specified in Unicode Standard Annex #15, Unicode Normalization Forms.
     * @param 形式 Applicable values: "NFC", "NFD", "NFKC", or "NFKD", If not specified default
     * is "NFC"
     */
    //@@{ 正常化:normalize, 形式:form }
    正常化(形式: "NFC" | "NFD" | "NFKC" | "NFKD"): 文字;
    /**
     * Returns the String value result of normalizing the string into the normalization form
     * named by form as specified in Unicode Standard Annex #15, Unicode Normalization Forms.
     * @param 形式 Applicable values: "NFC", "NFD", "NFKC", or "NFKD", If not specified default
     * is "NFC"
     */
    //@@{ 形式:form }
    正常化(形式?: 文字): 文字;
    /**
     * Returns a String value that is made from count copies appended together. If count is 0,
     * T is the empty String is returned.
     * @param 数量 number of copies to append
     */
    //@@{ 重复:repeat, 数量:count }
    重复(数量: 数字): 文字;
    /**
     * Returns true if the sequence of elements of searchString converted to a String is the
     * same as the corresponding elements of this object (converted to a String) starting at
     * position. Otherwise returns false.
     */
    //@@{ 开始包含:startsWith, 搜索文字:searchString, 位置:position }
    开始包含(搜索文字: 文字, 位置?: 数字): 真假;
    /**
     * Returns an <a> HTML anchor element and sets the name attribute to the text value
     * @param name
     */
    anchor(name: 文字): 文字;
    /** Returns a <big> HTML element */
    big(): 文字;
    /** Returns a <blink> HTML element */
    blink(): 文字;
    /** Returns a <b> HTML element */
    bold(): 文字;
    /** Returns a <tt> HTML element */
    fixed(): 文字;
    /** Returns a <font> HTML element and sets the color attribute value */
    fontcolor(color: 文字): 文字;
    /** Returns a <font> HTML element and sets the size attribute value */
    fontsize(size: 数字): 文字;
    /** Returns a <font> HTML element and sets the size attribute value */
    fontsize(size: 文字): 文字;
    /** Returns an <i> HTML element */
    italics(): 文字;
    /** Returns an <a> HTML element and sets the href attribute value */
    link(url: 文字): 文字;
    /** Returns a <small> HTML element */
    small(): 文字;
    /** Returns a <strike> HTML element */
    strike(): 文字;
    /** Returns a <sub> HTML element */
    sub(): 文字;
    /** Returns a <sup> HTML element */
    sup(): 文字;
}
//@@{ 文字构造函数类:StringConstructor }
接口 文字构造函数类 {
    /**
     * Return the String value whose elements are, in order, the elements in the List elements.
     * If length is 0, the empty string is returned.
     */
    //@@{ 从代码点:fromCodePoint, 代码点:codePoints }
    从代码点(...代码点: 数字[]): 文字;
    /**
     * String.raw is intended for use as a tag function of a Tagged Template String. When called
     * as such the first argument will be a well formed template call site object and the rest
     * parameter will contain the substitution values.
     * @param 模板 A well-formed template string call site representation.
     * @param 替换 A set of substitution values.
     */
    //@@{ 模板文字数组类:TemplateStringsArray }
    //@@{ 组成:raw, 模板:template, 替换:substitutions }
    组成(模板: 模板文字数组类, ...替换: 任意[]): 文字;
}
